# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppStore Card Transition' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppStore Card Transition
	pod "CollectionKit"	

  target 'AppStore Card TransitionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppStore Card TransitionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
